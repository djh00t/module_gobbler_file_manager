jobs:
  checkout:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
  minimal_workflow:
    name: Minimal Workflow for Debugging
    on:
      push:
        tags:
          - 'v[0-9]{1,3}\.[0-9]{1,3}\.[0-9]{1,3}'
  workflow_dispatch: {}